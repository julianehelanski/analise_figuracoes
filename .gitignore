# Configuração local (caminho do Drive em .env não é versionado)
.env

# PDFs nunca entram no repositório (copyright; ficam na pasta Drive sincronizada)
*.pdf
*.PDF

# Ambientes virtuais
.venv/
venv/
env/
ENV/

# Caches Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Modelos spaCy baixados localmente (instalados como pacotes via pip)
# Nada a ignorar aqui; spaCy instala em site-packages.

# IDEs
.vscode/
.idea/
*.swp
.DS_Store

# Outputs intermediários grandes (versionar só o que for citável)
outputs/figuras/**/*.svg.tmp
outputs/**/.cache/

# Jupyter
.ipynb_checkpoints/

# Passo 4 (15/05/2026): artefatos com reprodução extensa de Latour ficam fora do repositório público.
# Contêm trechos com ±50 palavras de contexto e curadoria de passagens citáveis para a tese.
# O script de geração está versionado em scripts/; os PDFs originais ficam no Drive local;
# rodar o script reproduz os arquivos abaixo localmente.
outputs/passo4/kwic_ampliado.csv
outputs/passo4/passagens_curadas.md
outputs/passo4/sequencia_exercito_ciencia.md
